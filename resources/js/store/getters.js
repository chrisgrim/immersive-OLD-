    let getters = {
         events: state => {
             return state.events
         },
         userSearchRequest: state => {
         	return state.userSearchRequest
         }
    }

    export default  getters